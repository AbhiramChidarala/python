#keyword arguments = an argument preceded by an identifier 
#                      helps with readability 
#                      order of argument doesn't matter 
#                      1. positional 2. default 3. KEYWORD 4. arbitrary                       

#def hello(greeting, title, first, last ):
#    print(f"{greeting} {title} {first} {last}")

#hello("Hello",title="mr", first="chidarala", last="Muralidhar")

#print("1","2","3","4",sep = "-")   # ssep function separate the elements with a separater 


#def get_phone(country, area , first , last ):
#    return f"{country}-{area}-{first}-{last}"

#phone_num = get_phone(country=91, area=897, first=884, last=40041)
#print(phone_num)